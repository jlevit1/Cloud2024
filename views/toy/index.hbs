<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Toy List</title>
   <!-- Bootstrap CSS -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
   <div class="container my-5">
      <div class="card">
         <div class="card-header text-center bg-primary text-white">
            <h1>PRODUCTS LIST</h1>
         </div>
         <div class="card-body">
            <div class="text-end mb-3">
               <a href="/toy/add" class="btn btn-success">Add New Toy</a>
            </div>
            <table class="table table-hover table-bordered">
               <thead class="table-primary">
                  <tr>
                     <th>Name</th>
                     <th>Price</th>
                     <th>Cover</th>
                     <th>Menu</th>
                  </tr>
               </thead>
               <tbody>
                  {{#each toys }}
                  <tr>
                     <td>{{ title }}</td>
                     <td>${{ price }}</td>
                     <td>
                        <a href="/toy/detail/{{ _id}}">
                           <img src="{{ cover }}" alt="toy cover" class="img-fluid rounded" width="100" height="120">
                        </a>
                     </td>
                     <td>
                        <a class="btn btn-info btn-sm" href="/toy/detail/{{ _id}}">Detail</a>
                        <a class="btn btn-warning btn-sm" href="/toy/edit/{{ _id}}">Edit</a>
                        <a class="btn btn-danger btn-sm" href="/toy/delete/{{ _id}}"
                           onclick="return confirm('Are you sure to delete this toy?')">Delete</a>
                     </td>
                  </tr>
                  {{/each}}
               </tbody>
            </table>
         </div>
      </div>
   </div>

   <!-- Bootstrap JS (Optional) -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
